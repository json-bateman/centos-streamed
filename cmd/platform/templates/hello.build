[Unit]
Description=Build the centos-streamed web image

[Build]
ImageTag={{.Image}}
SetWorkingDirectory={{.BuildContext}}
File=Containerfile

[Service]
# Building includes pulling the Go toolchain image and compiling, which can
# exceed the default start timeout on first run.
TimeoutStartSec=1800
